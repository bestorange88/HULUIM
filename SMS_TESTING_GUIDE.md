# SMS验证码测试指南

## 问题诊断

### 1. 用户收不到短信的可能原因

#### A. 短信签名问题（最可能）
- **当前签名**：【福建益品康丰集团】
- **需要做的**：
  1. 登录短信宝后台 (https://www.smsbao.com/)
  2. 进入"短信签名管理"
  3. 确认【福建益品康丰集团】签名已审核通过
  4. 如果未审核，需要提交审核（通常需要1-3个工作日）
  5. 如果签名未通过，可以临时使用已审核通过的其他签名

#### B. 手机号格式问题
- 系统已自动清理手机号格式（去除+86前缀和空格）
- 确保用户输入的是11位纯数字手机号

#### C. 运营商拦截
- 部分运营商会拦截营销类短信
- 建议用户：
  - 检查手机的垃圾短信文件夹
  - 检查手机安全软件是否拦截
  - 尝试使用其他运营商的手机号

#### D. 短信宝账户问题
- 确认短信宝账户余额充足
- 确认账户状态正常
- 检查是否有IP白名单限制

### 2. 万能验证码 911522

万能验证码已正确配置，使用方法：

#### 登录场景
1. 输入手机号（任意已注册的11位手机号）
2. 点击"发送验证码"（可以跳过实际发送）
3. 输入验证码：`911522`
4. 点击登录

#### 注册场景
1. 输入手机号（任意未注册的11位手机号）
2. 点击"发送验证码"（可以跳过实际发送）
3. 输入验证码：`911522`
4. 输入用户名、昵称、密码
5. 输入邀请码：`911522`（万能邀请码）
6. 点击注册

#### 注意事项
- 万能验证码在开发和生产环境都有效
- 万能验证码同时作为万能邀请码使用
- 使用万能验证码时无需实际发送短信
- 开发环境会在验证码输入框下方显示提示

## 调试步骤

### 1. 检查Edge Function日志

```bash
# 查看send-sms-code日志
supabase functions logs send-sms-code

# 查看sms-auth日志
supabase functions logs sms-auth
```

### 2. 浏览器控制台日志

打开浏览器控制台（F12），查看以下日志：
- 发送验证码时：`Sending SMS code to: [phone] purpose: [login/register]`
- 登录时：`Attempting SMS login with phone: [phone] code: [code]`
- 注册时：`Attempting SMS registration with phone: [phone] code: [code] inviteCode: [code]`

### 3. 验证短信宝API

手动测试短信宝API：
```bash
curl "https://api.smsbao.com/sms?u=YOUR_USERNAME&p=YOUR_PASSWORD&m=PHONE_NUMBER&c=【Alo生态】您的验证码是123456"
```

返回码说明：
- `0`: 成功
- `30`: 密码错误
- `40`: 账户余额不足
- `41`: 账户未激活
- `43`: IP地址限制
- `51`: 签名不正确

## 临时解决方案

如果短信签名审核需要时间，可以临时修改代码使用已审核的签名：

```typescript
// supabase/functions/send-sms-code/index.ts
// 将第64行的签名改为已审核通过的签名
const message = `【已审核的签名】您的验证码是${code}。有效期为10分钟，请尽快验证。`;
```

## 生产环境建议

1. **关闭万能验证码提示**
   - 目前提示仅在开发环境显示
   - 生产环境不会显示

2. **配置签名**
   - 确保【福建益品康丰集团】签名审核通过
   - 建议备用多个审核通过的签名

3. **监控短信发送**
   - 定期检查短信宝余额
   - 设置余额不足告警
   - 监控短信发送失败率

4. **用户友好提示**
   - 如果发送失败，提供更详细的错误信息
   - 告知用户可能需要等待1-2分钟
   - 提供重新发送选项（60秒冷却）
